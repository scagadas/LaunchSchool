// Multi String

// Problem:
// Assign the given strings into a single string, presevering the line break

// given string:
// A pirate I was meant to be!
// Trim the sails and roam the sea!

let theString = 'A pirate I was meant to be!\nTrim the sails and roam the sea!';
// the /n (new line) character can be used to put in a line break in a string
// the /t (tab) character will insert a tab whevere it is placed in a strings
console.log(theString);