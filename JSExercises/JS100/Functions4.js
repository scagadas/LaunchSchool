//Squared Number

//Write a function that accepts: a single numbter
//returns: the result of multiplying its argument by an exponent of 2 (squaring)


function squaredNumber (number) {
  return number * number
}

console.log(squaredNumber(2));