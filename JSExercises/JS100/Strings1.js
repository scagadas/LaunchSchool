// Length

// Problem:
// Determine the lenght of the following:

let countMe = "These aren't the droids you're looking for.";
// assigning the string to a variable
let countMeLength = countMe.length
// using the .length property to find the length of the string
console.log(countMeLength)
// logging the length number value to the console
