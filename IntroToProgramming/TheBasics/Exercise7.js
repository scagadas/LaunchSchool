let foo = ['a', 'b', 'c'];

console.log(foo.length)
console.log(foo[3]) //returns undefined since there is no item in array 3. Note arrays are zero indexed.

// No perticular reason given in the answer why error isn't given. Just have to know that it will return undefined
