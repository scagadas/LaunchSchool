function multiplyNumbers(num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);

/*
function arguments: 2, 3, 4

function body: 
  let result = num1 * num2 * num3;
  return results;

function declaration: the code defining the function (lines 1 - 4)
  
function invocation: multiplyNumbers(2, 3, 4)

function name: multiplyNumbers

function parameters: num1, num2, num3

function return value: result, which equals 24 when plugging in the given arguments

variable in the program: 
- multiplyNumbers
- num1, num2, num3 
- result
- product
*/